    {{#each kit}}
            <div class="grid">
               <img src="{{photo}}"> 
                 {{>food this}}
                <form method="POST" action="/load-data/addToCart/{{this.title}}">
                    <input type="submit" value="Buy" />
                </form>
            </div>
    {{/each}} 

